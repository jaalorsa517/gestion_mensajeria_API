column-center()
  display flex
  flex-direction column
  justify-content center
  align-items center

size-box(width,height)
  width width
  height height 


margin-sides(size)
  margin-right size
  margin-left size

margin-roof-floor(size)
  margin-top size
  margin-bottom size

shadow-box()
  box-shadow 2px 5px 10px color-fonts-opacity

shadow-text()
  text-shadow 1px 2px 5px color-fonts-opacity